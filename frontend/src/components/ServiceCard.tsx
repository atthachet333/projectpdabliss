import type { Service } from '../types';
interface ServiceCardProps { service:Service; detailed?:boolean; }
export function ServiceCard({service,detailed=false}:ServiceCardProps):JSX.Element { const Icon=service.icon; return <article className="card"><div className="flex items-start justify-between"><span className="grid h-12 w-12 place-items-center rounded-xl bg-green-50 text-green-700"><Icon/></span><span className="text-3xl font-extrabold text-slate-100">{service.id}</span></div><h3 className="mt-5 text-lg font-bold">{service.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{service.description}</p>{detailed&&<><p className="mt-4 text-sm font-semibold text-green-800">เอกสารเบื้องต้น</p><ul className="mt-2 list-inside list-disc text-sm text-slate-600">{service.documents.map(doc=><li key={doc}>{doc}</li>)}</ul><p className="mt-4 rounded-lg bg-slate-50 px-3 py-2 text-sm">ระยะเวลา: <strong>{service.estimatedTime}</strong></p></>}</article>; }
